<h1>Listing question_details</h1>

<table>
  <tr>
    <th>Question categorie</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @question_details.each do |question_detail| %>
  <tr>
    <td><%= question_detail.question_categorie_id %></td>
    <td><%= question_detail.name %></td>
    <td><%= link_to 'Show', question_detail %></td>
    <td><%= link_to 'Edit', edit_question_detail_path(question_detail) %></td>
    <td><%= link_to 'Destroy', question_detail, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Question detail', new_question_detail_path %>
