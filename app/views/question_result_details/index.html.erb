<h1>Listing question_result_details</h1>

<table>
  <tr>
    <th>Question detail</th>
    <th>Question result</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @question_result_details.each do |question_result_detail| %>
  <tr>
    <td><%= question_result_detail.question_detail_id %></td>
    <td><%= question_result_detail.question_result_id %></td>
    <td><%= link_to 'Show', question_result_detail %></td>
    <td><%= link_to 'Edit', edit_question_result_detail_path(question_result_detail) %></td>
    <td><%= link_to 'Destroy', question_result_detail, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Question result detail', new_question_result_detail_path %>
