<%= form_for(@question_result) do |f| %>
  <% if @question_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_result.errors.count, "error") %> prohibited this question_result from being saved:</h2>

      <ul>
      <% @question_result.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_base_id %><br />
    <%= f.text_field :question_base_id %>
  </div>
  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.text_field :question_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
